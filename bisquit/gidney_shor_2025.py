# author: Suhas Vittal
# date: 20 July 2026

from common import *
import random

#################################################################
#################################################################

def _loop1_unary_iteration(q_k: str, 
                           q_k_w: int,
                           q_dlog_delta: str,
                           q_dlog_delta_w: int,
                           unary_it_anc: str,
                           unary_it_anc_w: int
):
    pass

def loop1(q_dlog: str, 
          q_dlog_w: int, 
          q_k: str, 
          q_k_w: int, 
          q_dlog_delta: str, 
          q_dlog_delta_w: int,
          unary_it_anc: str,
          unary_it_anc_w: int
):
    '''
        Implements the lookup + add in loop1. 
            `q_dlog` is the register for the discrete log
            `q_k` is the index register into the lookup table
            `q_dlog_delta` is the register for the table lookup outcome
            `unary_it_anc` is the ancilla register for unary iteration
            `*_w` is the width of the corresponding register
    '''

    

#################################################################
#################################################################
